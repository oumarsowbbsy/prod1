{% extends 'base.html.twig' %}

{% block title %}Edit Tourisme{% endblock %}

{% block body %}
    <h1>Edit Tourisme</h1>

    {{ include('tourisme/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('tourisme_index') }}">back to list</a>

    {{ include('tourisme/_delete_form.html.twig') }}
{% endblock %}
